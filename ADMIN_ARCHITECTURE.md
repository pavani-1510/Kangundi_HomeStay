# 🎯 Admin System Architecture Diagram

## User Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     Login Page (/login)                         │
│                    [Same for All Users]                         │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      ▼
            Enter credentials (username, password)
                      │
                      ▼
         ┌────────────────────────────┐
         │  GET from users table      │
         │  WHERE username = input    │
         └────────────────┬───────────┘
                          │
                          ▼
              ┌─────────────────────────────┐
              │ Check password hash         │
              │ (werkzeug.security)         │
              └────────┬────────────────────┘
                       │
             ┌─────────┴──────────┐
             │                    │
      Password Match       Password Wrong
             │                    │
             ▼                    ▼
      Check is_admin     Flash: Invalid
             │           credentials
         ┌───┴────┐            │
         │        │            ▼
      TRUE      FALSE      Redirect /login
         │        │
         │        ▼
         │   Redirect /dashboard
         │   (User Dashboard)
         │
         ▼
    Redirect /admin/dashboard
    (Admin Dashboard)
         │
         ▼
    Store in session:
    - session['username']
    - session['is_admin'] = TRUE
```

## Route Protection Flow

```
┌──────────────────────────────────────────────────────┐
│  User tries to access protected admin route         │
│  e.g., /admin/dashboard                             │
└────────────────────┬─────────────────────────────────┘
                     │
                     ▼
         ┌────────────────────────┐
         │ @admin_required        │
         │ decorator checks       │
         └────────┬───────────────┘
                  │
        ┌─────────┴──────────┐
        │                    │
   is_admin == TRUE?    is_admin == FALSE?
        │                    │
        ▼                    ▼
   Continue to          Flash: "You do not have
   route handler         permission to access"
        │                    │
        ▼                    ▼
   Display page        Redirect /dashboard
   with data
```

## Admin Dashboard Data Flow

```
┌────────────────────────────────────────────────────┐
│         Admin logs in successfully                 │
│  /admin/dashboard (GET) via @admin_required        │
└───────────────────┬────────────────────────────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │ Fetch from Supabase: │
         │ homestays.select()   │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────────┐
         │ Get all homestay records │
         │ (id, name, owner, rooms, │
         │  beds, rating, price)    │
         └──────────┬───────────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │ Render template with:│
         │ - homestays list     │
         │ - stats cards        │
         │ - action buttons     │
         └──────────┬───────────┘
                    │
                    ▼
        ┌───────────────────────────┐
        │   admin_dashboard.html    │
        │   - Table of properties   │
        │   - Edit buttons          │
        │   - Delete buttons        │
        │   - Add button            │
        └───────────────────────────┘
```

## Add/Edit Homestay Flow

```
┌──────────────────────────────────┐
│  Admin clicks Add/Edit button    │
└────────────┬─────────────────────┘
             │
             ▼
    ┌─────────────────────────┐
    │ GET /admin/homestay/add │
    │ or                      │
    │ GET /admin/homestay/    │
    │     <id>/edit           │
    └────────┬────────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │ Fetch homestay data (if  │
    │ edit mode)               │
    └────────┬─────────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │ Render form with:        │
    │ - Empty fields (add) OR  │
    │ - Pre-filled (edit)      │
    │ - Validation             │
    └────────┬─────────────────┘
             │
             ▼
    Admin fills form & submits
             │
             ▼
    ┌──────────────────────────┐
    │ POST to:                 │
    │ /admin/homestay/add  OR  │
    │ /admin/homestay/<id>/edit│
    └────────┬─────────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │ Validate form data       │
    │ - Check required fields  │
    │ - Validate data types    │
    └────────┬─────────────────┘
             │
    ┌────────┴────────┐
    │                 │
  Valid            Invalid
    │                 │
    ▼                 ▼
Supabase          Flash error
INSERT/UPDATE     message
    │                 │
    ▼                 ▼
Success flash    Render form
message          again with
    │            error
    ▼
Redirect /admin/
dashboard
```

## Delete Homestay Flow

```
┌────────────────────────────────┐
│  Admin clicks Delete button    │
│  on homestay row               │
└─────────────┬──────────────────┘
              │
              ▼
    ┌───────────────────────┐
    │ Show confirmation     │
    │ dialog (JavaScript)   │
    └──────┬────────────────┘
           │
    ┌──────┴─────────┐
    │                │
 Cancel          Confirm
    │                │
    ▼                ▼
 Stay on          POST
 page        /admin/homestay/
             <id>/delete
             │
             ▼
    ┌─────────────────────┐
    │ Supabase DELETE:    │
    │ WHERE id = <id>     │
    └────────┬────────────┘
             │
             ▼
    ┌──────────────────────┐
    │ Homestay removed     │
    │ from database        │
    └────────┬─────────────┘
             │
             ▼
    ┌──────────────────────┐
    │ Flash success        │
    │ message              │
    └────────┬─────────────┘
             │
             ▼
    Redirect /admin/dashboard
             │
             ▼
    Table refreshed without
    deleted property
```

## Database Schema Diagram

```
┌─────────────────────────────────────────┐
│              users TABLE                │
├─────────────────────────────────────────┤
│ PK  id: UUID                            │
│     username: TEXT (UNIQUE)             │
│     email: TEXT (UNIQUE)                │
│     password_hash: TEXT                 │
│ NEW is_admin: BOOLEAN (DEFAULT FALSE)   │
│     created_at: TIMESTAMP               │
└─────────────────────────────────────────┘
          │
          │ (Admin records)
          │
     ┌────┴────┐
     │          │
  admin    regular
  user     users
(is_admin=(is_admin=
  TRUE)     FALSE)
     │          │
     ▼          ▼
Access /    Access /
admin/*     dashboard/*


┌─────────────────────────────────────────┐
│            homestays TABLE              │
├─────────────────────────────────────────┤
│ PK  id: SERIAL                          │
│     name: TEXT                          │
│     owner: TEXT                         │
│     rooms: INTEGER                      │
│     beds: INTEGER                       │
│     floor: INTEGER                      │
│     rating: DECIMAL                     │
│     reviews: INTEGER                    │
│     image: TEXT                         │
│     images: TEXT[] (array)              │
│     features: TEXT[] (array)            │
│     description: TEXT                   │
│     price: INTEGER                      │
│     contact: TEXT                       │
│     created_at: TIMESTAMP               │
└─────────────────────────────────────────┘
          │
          │ (CRUD operations)
          │
     ┌────┼────┬────┐
     │    │    │    │
   GET  INSERT UPDATE DELETE
   All   New  Existing Homestay
 Listed Homestay
```

## Role-Based Access Control (RBAC)

```
┌─────────────────┐
│  Admin User     │
│ is_admin=TRUE   │
└────────┬────────┘
         │
    ┌────┴────┬──────────┬───────────┬───────────┐
    │          │          │           │           │
    ▼          ▼          ▼           ▼           ▼
  /login   /dashboard  /rooms   /admin/*  /homestay/<id>
   ✅         ✅         ✅        ✅          ✅
   │          │          │           │
   └──────────┴──────────┴───────────┴──────────
                        │
              ┌─────────┴──────────┐
              │                    │
           CAN access          CAN access
           admin routes        user routes
              │                    │
              ▼                    ▼
         /admin/dashboard    /dashboard
         /admin/homestay/add /rooms
         /admin/edit/<id>    /homestay/<id>
         /admin/delete/<id>


┌──────────────────┐
│  Regular User    │
│ is_admin=FALSE   │
└────────┬─────────┘
         │
    ┌────┴────┬──────────┬───────────┬───────────┐
    │          │          │           │           │
    ▼          ▼          ▼           ▼           ▼
  /login   /dashboard  /rooms   /admin/*  /homestay/<id>
   ✅         ✅         ✅        ❌          ✅
   │          │          │           │
   └──────────┴──────────┴───────────┴──────────
                        │
              ┌─────────┴──────────┐
              │                    │
           CANNOT access      CAN access
           admin routes      user routes
              │                    │
              ▼                    ▼
         Error 403:          /dashboard
         Permission          /rooms
         Denied          /homestay/<id>
```

## File Structure Overview

```
Kangundi_HomeStay/
│
├── app.py [UPDATED]
│   ├── Admin decorator: @admin_required
│   ├── New routes:
│   │   ├── /admin/dashboard
│   │   ├── /admin/homestay/add
│   │   ├── /admin/homestay/<id>/edit
│   │   └── /admin/homestay/<id>/delete
│   └── Updated login logic
│
├── database_schema.sql [UPDATED]
│   ├── users table with is_admin
│   ├── Admin user creation
│   └── Sample homestay data
│
├── templates/
│   ├── admin_dashboard.html [NEW]
│   │   └── Table view + add button
│   ├── admin_add_homestay.html [NEW]
│   │   └── Add form
│   ├── admin_edit_homestay.html [NEW]
│   │   └── Edit form
│   └── [other existing templates]
│
├── Documentation:
│   ├── ADMIN_QUICK_START.md [NEW]
│   ├── ADMIN_FEATURE.md [NEW]
│   ├── ADMIN_IMPLEMENTATION.md [NEW]
│   ├── ADMIN_COMPLETE.md [NEW]
│   ├── ADMIN_SUMMARY.md [NEW]
│   └── ADMIN_CHECKLIST.md [NEW]
│
└── [other project files]
```

---

## 🎯 Summary

**Login → Role Check → Route Protection → Database Operations**

All through Supabase with proper authentication and authorization! 🔐
